<div class="card w-45">
  <div class="card-body">
    <h5 class="card-title"><%= board.title %></h5>
    <p class="card-text"><%= board.description %></p>
    <p class="card-text"><%= board.created_at %><p>
    <p class="card-text"><%= board.status %><p>
    <%= link_to "View Board", board ,class:"btn btn-primary" %>
  </div>
</div>